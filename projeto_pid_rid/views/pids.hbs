<h2 class="text-center mb-4">Meus PIDs</h2>

<h4>Bem-vindo, {{docente.nome}}!</h4>

<!-- Exibir os PIDs em formato de tabela -->
{{#if pids}}
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Ano</th>
                <th>Semestre</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            {{#each pids}}
                <tr>
                    <td>{{this.ano}}</td>
                    <td>{{this.semestre}}</td>
                    <td>
                        <button class="btn btn-primary">Editar</button>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <p>Você ainda não cadastrou nenhum PID.</p>
{{/if}}

<!-- Botão para cadastrar um novo PID -->
<a href="/projeto_pid_rid/cadastrarPid" class="btn btn-success">Cadastrar Novo PID</a>

<h2 class="text-center mb-4">Meus RIDs</h2>

<!-- Botão para cadastrar um novo RID -->
<a href="/projeto_pid_rid/cadastrarRid" class="btn btn-success">Cadastrar Novo RID</a>

<!-- Exibir os RIDs em formato de tabela -->
{{#if rids}}
    <table class="table table-bordered mt-4">
        <thead>
            <tr>
                <th>Ano</th>
                <th>Semestre</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            {{#each rids}}
                <tr>
                    <td>{{this.ano}}</td>
                    <td>{{this.semestre}}</td>
                    <td>
                        <button class="btn btn-primary">Editar</button>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <p>Você ainda não cadastrou nenhum RID.</p>
{{/if}}
